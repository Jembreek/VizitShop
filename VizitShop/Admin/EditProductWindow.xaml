<Window x:Class="VizitShop.EditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VizitShop"
        mc:Ignorable="d"
        Title="Редактирование товара"
        Height="500" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#F5F5F5">

    <Window.Resources>
        <Color x:Key="PrimaryColor">#7C4DFF</Color>
        <Color x:Key="DarkPrimaryColor">#5D36C2</Color>
        <Color x:Key="AccentColor">#FF4081</Color>
        <Color x:Key="DarkGray">#212121</Color>
        <Color x:Key="MediumGray">#757575</Color>
        <Color x:Key="LightGray">#BDBDBD</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="DarkPrimaryBrush" Color="{StaticResource DarkPrimaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="DarkGrayBrush" Color="{StaticResource DarkGray}"/>
        <SolidColorBrush x:Key="MediumGrayBrush" Color="{StaticResource MediumGray}"/>
        <SolidColorBrush x:Key="LightGrayBrush" Color="{StaticResource LightGray}"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0 0 0 2"/>
            <Setter Property="Padding" Value="8 12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
            <Setter Property="Margin" Value="0 0 0 15"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12 8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                CornerRadius="6" 
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkPrimaryBrush}"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="{StaticResource PrimaryBrush}"
                CornerRadius="10 10 0 0"
                Height="60" 
                VerticalAlignment="Top"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <DockPanel>
                <TextBlock Text="Редактирование товара" 
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="15 0 0 0"
                           DockPanel.Dock="Left"/>

                <Button Content="✕" 
                        Width="60" 
                        Height="60"
                        Background="Transparent"
                        Foreground="White"
                        FontSize="18"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Click="CloseButton_Click"
                        Cursor="Hand"
                        DockPanel.Dock="Right"
                        Margin="0,0,-138,0" RenderTransformOrigin="0.505,0.521">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Grid>
                                            <Ellipse x:Name="BackgroundCircle"
                                                     Width="24" 
                                                     Height="24"
                                                     Fill="Transparent"
                                                     Opacity="0"/>
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BackgroundCircle" Property="Opacity" Value="0.2"/>
                                                <Setter TargetName="BackgroundCircle" Property="Fill" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </DockPanel>
        </Border>

        <Border Background="White" 
                Margin="0,60,0,0"
                CornerRadius="0 0 12 12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Название товара" 
                   Foreground="{StaticResource MediumGrayBrush}"
                   Margin="0 0 0 5"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                 Text="{Binding Product.Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Цена" 
                   Foreground="{StaticResource MediumGrayBrush}"
                   Margin="0 0 0 5"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                 Text="{Binding Product.Price, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F0}}"/>

                    <TextBlock Text="Бренд" 
                   Foreground="{StaticResource MediumGrayBrush}"
                   Margin="0 0 0 5"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                 Text="{Binding Product.Brand, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Размер" 
                   Foreground="{StaticResource MediumGrayBrush}"
                   Margin="0 0 0 5"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                 Text="{Binding Product.Size, UpdateSourceTrigger=PropertyChanged}"/>

                    <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0">
                        <Button Content="Сохранить" 
                    Style="{StaticResource ModernButton}"
                    Width="120"
                    Click="SaveButton_Click"
                    IsDefault="True"/>
                        <Button Content="Отмена" 
                    Style="{StaticResource ModernButton}"
                    Background="{StaticResource LightGrayBrush}"
                    Foreground="{StaticResource DarkGrayBrush}"
                    Width="120"
                    Margin="10 0 0 0"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>